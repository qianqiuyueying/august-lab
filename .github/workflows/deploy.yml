name: Deploy

on:
  push:
    branches: [master, main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: 构建 & 部署
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: 安装依赖 & 构建
        run: |
          npm ci --no-audit
          npx prisma generate
          npm run build

      - name: 配置 SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 部署到服务器
        run: |
          SERVER="root@192.144.154.17"

          echo "::group::准备目录"
          ssh ${SERVER} "mkdir -p /opt/august-lab"

          echo "::group::同步构建产物 + prisma"
          rsync -az --delete --info=progress2 \
            .next/standalone/ \
            "${SERVER}:/opt/august-lab/"
          rsync -az --delete --info=progress2 \
            .next/static/ \
            "${SERVER}:/opt/august-lab/.next/static/"
          rsync -az --info=progress2 \
            public/ \
            "${SERVER}:/opt/august-lab/public/"
          rsync -az --info=progress2 \
            prisma/ \
            "${SERVER}:/opt/august-lab/prisma/"

          echo "::group::配置并重启"
          ssh ${SERVER} 'bash -s' << 'ENDSCRIPT'
            cd /opt/august-lab

            # 写入环境变量（仅首次，后续不覆盖）
            if [ ! -f .env ]; then
              echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > .env
              echo "ADMIN_PASSWORD=${{ secrets.ADMIN_PASSWORD }}" >> .env
            fi

            # 安装 PM2（如果还没有）
            which pm2 || npm i -g pm2

            # 迁移数据库
            npx prisma migrate deploy

            # 重启应用
            pm2 delete august-lab 2>/dev/null || true
            pm2 start server.js --name august-lab
            pm2 save

            sleep 2
            curl -sf -o /dev/null -w "HTTP %{http_code}\n" http://localhost:3000 && echo "OK" || echo "等待..."
          ENDSCRIPT
