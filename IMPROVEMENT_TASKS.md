# 项目改进任务列表

## 第一阶段：高优先级（安全与稳定性）- 建议立即执行

### 任务 1.1: 修复认证系统硬编码凭据问题
- **目标**: 移除 `backend/app/auth.py` 中的硬编码用户凭据，完全使用数据库用户模型
- **修改文件**: `backend/app/auth.py`, `backend/app/models.py` (如需调整)
- **验收标准**:
  - [ ] 登录接口只查询数据库验证用户
  - [ ] 移除所有硬编码的用户名/密码检查逻辑
  - [ ] 现有测试用例全部通过
  - [ ] 添加新用户注册后能正常登录的集成测试
- **预计工作量**: 2-3 小时
- **风险**: 低（需确保测试覆盖）

### 任务 1.2: 完善 JWT Token 响应格式
- **目标**: 在 `/api/auth/login` 响应中明确返回 `token_type` 字段
- **修改文件**: `backend/app/auth.py`
- **验收标准**:
  - [ ] 登录响应包含 `{"access_token": "...", "token_type": "bearer"}`
  - [ ] 更新 API 文档或注释
  - [ ] 前端兼容性验证（如有需要）
- **预计工作量**: 0.5 小时
- **风险**: 极低

### 任务 1.3: 收紧 CORS 配置
- **目标**: 限制 CORS 允许的源为生产环境实际域名
- **修改文件**: `backend/main.py`
- **验收标准**:
  - [ ] 开发环境允许 `localhost` 和 `127.0.0.1`
  - [ ] 生产环境仅允许配置的实际域名（通过环境变量）
  - [ ] 移除通配符 `*` 配置
  - [ ] 添加 CORS 配置的单元测试
- **预计工作量**: 1-2 小时
- **风险**: 中（需确认前端部署域名）

### 任务 1.4: 添加登录速率限制基础实现
- **目标**: 防止暴力破解攻击，限制同一 IP/用户的登录尝试频率
- **修改文件**: `backend/app/auth.py`, `backend/main.py`
- **验收标准**:
  - [ ] 同一 IP 在 5 分钟内最多尝试登录 10 次
  - [ ] 超过限制后返回 429 状态码
  - [ ] 添加清晰的错误提示信息
  - [ ] 添加速率限制的单元测试
- **预计工作量**: 3-4 小时
- **风险**: 中（需平衡安全性与用户体验）

---

## 第二阶段：中优先级（架构与质量）- 建议短期规划

### 任务 2.1: 解决数据库 Engine 全局变量问题
- **目标**: 重构数据库初始化，支持测试隔离和多实例场景
- **修改文件**: `backend/app/database.py`, `backend/tests/conftest.py`
- **验收标准**:
  - [ ] 移除全局 `engine` 变量
  - [ ] 使用依赖注入传递数据库会话
  - [ ] 测试使用独立的测试数据库或事务回滚
  - [ ] 所有现有测试通过且相互隔离
- **预计工作量**: 4-6 小时
- **风险**: 中高（涉及核心架构改动）

### 任务 2.2: 补充关键路径单元测试
- **目标**: 提高工具函数和核心逻辑的测试覆盖率
- **修改文件**: `backend/tests/test_utils.py` (新建), 其他测试文件
- **验收标准**:
  - [ ] `verify_password`, `create_access_token` 等工具函数有独立单元测试
  - [ ] 核心业务逻辑测试覆盖率达到 80%+
  - [ ] 添加边界条件和异常场景测试
- **预计工作量**: 4-5 小时
- **风险**: 低

### 任务 2.3: 统一和完善类型注解
- **目标**: 为所有公共函数和 API 端点添加完整的类型注解
- **修改文件**: `backend/app/*.py`
- **验收标准**:
  - [ ] 所有函数参数和返回值有类型注解
  - [ ] 使用 `mypy` 或类似工具检查无类型错误
  - [ ] 复杂类型使用 `TypedDict` 或 `dataclass` 明确结构
- **预计工作量**: 3-4 小时
- **风险**: 低

### 任务 2.4: 添加 Docker 健康检查
- **目标**: 使容器编排工具能检测服务健康状态
- **修改文件**: `backend/Dockerfile`, `docker-compose.yml`
- **验收标准**:
  - [ ] Dockerfile 中添加 `HEALTHCHECK` 指令
  - [ ] 健康检查端点 `/health` 返回服务状态
  - [ ] docker-compose 配置健康检查参数
  - [ ] 验证 `docker inspect` 能看到健康状态
- **预计工作量**: 1-2 小时
- **风险**: 低

---

## 第三阶段：低优先级（优化与体验）- 按需执行

### 任务 3.1: 添加 Nginx 安全响应头
- **目标**: 增强前端安全性，防止常见 Web 攻击
- **修改文件**: `frontend/nginx.conf`
- **验收标准**:
  - [ ] 添加 `X-Frame-Options`, `X-Content-Type-Options`, `X-XSS-Protection` 等头部
  - [ ] 配置 `Content-Security-Policy` (CSP)
  - [ ] 验证浏览器安全扫描无高风险项
- **预计工作量**: 2-3 小时
- **风险**: 中（CSP 配置需谨慎测试）

### 任务 3.2: 实现 Refresh Token 机制
- **目标**: 提升用户体验，避免频繁重新登录
- **修改文件**: `backend/app/auth.py`, `backend/app/models.py`
- **验收标准**:
  - [ ] 登录时同时返回 access_token 和 refresh_token
  - [ ] 添加 `/api/auth/refresh` 端点刷新 token
  - [ ] refresh_token 可撤销（登出时）
  - [ ] 添加 token 黑名单或过期策略
- **预计工作量**: 5-7 小时
- **风险**: 中（增加系统复杂度）

### 任务 3.3: 前端测试全覆盖
- **目标**: 建立前端自动化测试体系
- **修改文件**: `frontend/src/__tests__/` (新建)
- **验收标准**:
  - [ ] 关键组件有单元测试（React Testing Library）
  - [ ] 关键用户流程有 E2E 测试（Playwright/Cypress）
  - [ ] 测试集成到 CI 流程
- **预计工作量**: 8-12 小时
- **风险**: 低（但工作量大）

### 任务 3.4: 优化 Docker 镜像构建
- **目标**: 减小镜像体积，加快构建速度
- **修改文件**: `backend/Dockerfile`, `frontend/Dockerfile`
- **验收标准**:
  - [ ] 使用多阶段构建
  - [ ] 镜像体积减少 30%+
  - [ ] 构建时间减少 20%+
  - [ ] 合理使用缓存层
- **预计工作量**: 2-3 小时
- **风险**: 低

---

## 执行建议

### 推荐执行顺序
1. **必做（第一阶段全部）**: 任务 1.1 → 1.2 → 1.3 → 1.4
2. **强烈建议（第二阶段核心）**: 任务 2.1 → 2.2 → 2.4
3. **可选（第二/三阶段剩余）**: 根据项目需求选择性执行

### 不推荐立即执行的任务
- 任务 3.1 (Nginx 安全头): 除非有明确安全合规要求
- 任务 3.3 (前端测试): 工作量大，可在团队资源充足时进行
- 任务 3.2 (Refresh Token): 如当前 token 过期策略已满足需求，可延后

### 总体预估
- **第一阶段**: 7-10 小时
- **第二阶段（核心）**: 9-13 小时
- **第三阶段（可选）**: 15-22 小时

---

## 审查确认

请审查以上任务列表，确认：
1. 任务划分是否合理？
2. 优先级排序是否符合你的预期？
3. 是否有需要调整或补充的任务？
4. 是否批准开始执行第一阶段任务？

**等待您的审批后开始执行。**
